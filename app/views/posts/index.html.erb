<% @posts.each do |post| %>
    <div class="card container">
        <div class="card-body">
        <h3><%= post.title %></h3>
        <!--<p><%= post.body %></p>-->
        <%= link_to "Read More", post_path(post), :class => "btn btn-primary btn-sm" %>
        </div>
    </div>
<% end %>